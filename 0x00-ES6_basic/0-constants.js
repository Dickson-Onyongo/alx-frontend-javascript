export taskFirst = () => {
	const task = 'I prefer const when I can.';
	return task;
}
export getLast = () => ('is okay');

export taskNext = () => {
	let combination = 'But sometimes let';
	combnation += getLast();

	return combination;
}

